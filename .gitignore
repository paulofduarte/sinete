# SPDX-FileCopyrightText: 2026 Paulo Duarte
# SPDX-License-Identifier: Apache-2.0

# Zig build output
/zig-out/
/.zig-cache/

# Signed app bundle (scripts/bundle.sh output)
/dist/

# kcov coverage output
/kcov-out/

# devenv / direnv (optional dev env; the repo builds with `zig build` alone)
/.devenv/
/.devenv.flake.nix
/devenv.local.nix
/.direnv/

# Claude local specs / working notes (not published)
/.claude/

# Local MCP config (personal tooling, absolute paths)
/.mcp.json

# Editor / OS
.DS_Store

# stray local package mirror (zig uses the global cache)
/zig-pkg/
