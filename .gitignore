# Go build output
/sinete
*.test
*.out

# Nix build output
/result
/result-*

# Generated by git-hooks.nix on entering the dev shell (hooks live in the flake)
/.pre-commit-config.yaml

# Build / release artifacts (the signed bundle is written under dist/ — #13)
/dist/

# Signing material (never commit)
*.provisionprofile
*.p12
*.key
*.certSigningRequest

# Claude local specs / working notes (not published)
/.claude/

# Local MCP config (personal tooling, absolute paths)
/.mcp.json

# Editor / OS
.DS_Store
